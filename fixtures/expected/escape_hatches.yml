name: Escape Hatches
on:
  push:
    branches:
    - main
jobs:
  typed:
    runs-on: ubuntu-latest
    steps:
    - name: Custom shell
      run: echo hello
      shell: custom-shell
    snapshot:
      image-name: custom-image
      version: latest
    custom-timeout: 30
  raw:
    runs-on: ubuntu-latest
    steps:
    - run: echo 'raw job'
x-generated-by: ghagen

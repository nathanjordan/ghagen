# The one exit-code contract. Both ports drive this through main().
#
#   0  success
#   1  expected failure (stale files, unresolved refs, config not found)
#   2  usage error (unknown command/option, missing or invalid option value)
#
# Most rows are project-independent: they load no user config module, so the
# driver only has to chdir into an empty directory.
#
# A row MAY carry an optional `project:` key naming a directory under
# `fixtures/cli-exit-code-projects/`. The driver copies that directory into a
# fresh temp dir and chdirs there instead of into an empty one. This exists
# because the codes that depend on *user code running* -- a config module that
# raises -- are structurally invisible to an empty-directory driver, and that
# is exactly the class in which the two ports drifted (Python propagated the
# exception where TypeScript returned 1). A fixture project holds BOTH ports'
# entrypoints; their search paths are disjoint, so each port discovers its own
# and never sees the other's.
- id: help
  argv: ["--help"]
  exit: 0
- id: no-args
  argv: []
  exit: 2
- id: subapp-no-subcommand
  argv: ["deps"]
  exit: 2
- id: unknown-command
  argv: ["bogus-command"]
  exit: 2
- id: unknown-option
  argv: ["synth", "--bogus"]
  exit: 2
- id: missing-option-argument
  argv: ["init", "--outdir"]
  exit: 2
- id: bad-format-value
  argv: ["deps", "upgrade", "--format", "bogus"]
  exit: 2
- id: bad-mode-value
  argv: ["deps", "upgrade", "--mode", "bogus"]
  exit: 2
- id: config-not-found-discovery
  argv: ["synth"]
  exit: 1
- id: config-not-found-flag
  argv: ["synth", "--config", "does-not-exist.py"]
  exit: 1
# `check-synced` is the other config-consuming command; the rows above only
# ever exercised `synth`'s copy of the discovery path.
- id: config-not-found-check-synced
  argv: ["check-synced"]
  exit: 1
# `help` is not a command in either port. commander ships an implicit one and
# used to answer 0 here while Typer answered 2; the ports now agree that the
# documented spelling is `--help` and a bare `help` is a usage error.
- id: bare-help-word
  argv: ["help"]
  exit: 2
# ghagen declares no `--version`. Asserted in the Python renderer tests but
# absent from the shared table, so nothing held the TypeScript port to it.
- id: unknown-top-level-option
  argv: ["--version"]
  exit: 2
# Unknown command *inside a sub-app*. `unknown-command` above only covers the
# root; the `deps` subtree is mounted differently in the TypeScript port
# (`addCommand`, not `command`), so it is a genuinely distinct path.
- id: unknown-subcommand
  argv: ["deps", "bogus-subcommand"]
  exit: 2
# Help for a subcommand, not the root.
- id: subcommand-help
  argv: ["synth", "--help"]
  exit: 0
# The class the empty-directory rows structurally cannot reach: user code runs
# and raises. Python propagated the exception here instead of returning a code.
- id: config-module-raises
  argv: ["synth"]
  project: config-raises
  exit: 1
# `deps update`'s own flag validation. `bad-format-value` and `bad-mode-value`
# above only cover `deps upgrade`; `deps update` validates a different and
# larger set (`--mode`, `--output`, `--format`) in its own hand-written
# `if`-ladder, and every one of those checks was asserted only inside each
# port's own test file. A port that dropped a check, or answered 1 instead of
# 2, drifted silently. See docs/issues/33.
#
# Every row below runs in an empty directory and still reaches its check,
# which is itself part of the contract: `deps update` validates its flags
# *before* config discovery, so a bad flag is a usage error (2) and never
# gets downgraded to "no config file found" (1).
- id: bad-update-mode-value
  argv: ["deps", "update", "--mode", "bogus"]
  exit: 2
- id: bad-update-output-value
  argv: ["deps", "update", "--output", "bogus"]
  exit: 2
- id: bad-update-format-value
  argv: ["deps", "update", "--format", "bogus"]
  exit: 2
# The three newline rejections. These are not cosmetic: each of these values
# is a workflow-author-supplied action input that reaches `$GITHUB_OUTPUT` as
# the right-hand side of a `key=value` line, so an embedded newline forges
# additional outputs. Rejecting at the edge is what lets `render_update_plan`
# / `renderUpdatePlan` stay single-line per field and skip the heredoc
# delimiter machinery entirely -- which means a port that stopped rejecting
# would not fail any rendering test, only this one.
- id: newline-in-branch-prefix
  argv: ["deps", "update", "--branch-prefix", "ghagen-update/\nname=forged"]
  exit: 2
- id: newline-in-commit-message-prefix
  argv: ["deps", "update", "--commit-message-prefix", "chore:\nname=forged"]
  exit: 2
- id: newline-in-labels
  argv: ["deps", "update", "--labels", "ci\nname=forged"]
  exit: 2

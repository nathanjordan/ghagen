# This file is generated by ghagen. Do not edit manually.
name: Release
on:
  push:
    tags:
    - v*
permissions:
  id-token: write
jobs:
  publish:
    runs-on: ubuntu-latest
    environment:
      name: release
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 1
    - name: Set up uv
      uses: astral-sh/setup-uv@v4
    - name: Build
      run: uv build
    - name: Publish to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
